---
export interface Props {
  class?: string;
}

const { class: className = "text-2xl" } = Astro.props;
---

<a href="/" aria-label="packaged logo">
  <span class={className} style={{ fontFamily: "Jaro, sans-serif" }}>
    packaged
  </span>
</a>
